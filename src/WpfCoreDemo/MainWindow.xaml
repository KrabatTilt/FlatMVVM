<Window x:Class="WpfCoreDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCoreDemo"
        mc:Ignorable="d"
        Title="FlatMVVM Wpf Examples" 
        Width="1920" Height="1080" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance local:MainVM, IsDesignTimeCreatable = True}">
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="200" />
        <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <ListView Grid.Column="0"
              ItemsSource="{Binding DemoCases}"
              DisplayMemberPath="Name"
              SelectedItem="{Binding ActiveView}"/>

    <ContentControl Grid.Column="1" 
                    Content="{Binding ActiveView}"
                    ContentTemplateSelector="{Binding DemoCaseContentTemplateSelector}"/>

</Grid>
</Window>
